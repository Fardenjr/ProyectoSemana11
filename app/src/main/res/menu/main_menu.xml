<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- Perfil del usuario -->
    <item
        android:id="@+id/action_perfil"
        android:icon="@drawable/ic_baseline_person_24"
        android:title="@string/menu_title_details"
        app:showAsAction="ifRoom" />

    <!-- Cerrar sesión -->
    <item
        android:id="@+id/action_logout"
        android:icon="@drawable/ic_baseline_logout_24"
        android:title="@string/menu_title_logout"
        android:orderInCategory="100"
        app:showAsAction="never" />

    <!-- Chat Bot -->
    <item
        android:id="@+id/action_chatbot"
        android:title="Chat Bot"
        android:icon="@drawable/ic_baseline_list_24"
        app:showAsAction="never" />

    <!-- Ver solicitudes (solo admin) -->
    <item
        android:id="@+id/action_ver_solicitudes"
        android:title="Ver Solicitudes"
        android:icon="@drawable/ic_baseline_list_24"
        app:showAsAction="ifRoom" />

    <!-- 🆕 Chat en tiempo real -->
    <item
        android:id="@+id/action_chat"
        android:title="Chat en tiempo real"
        android:icon="@drawable/ic_chat"
        app:showAsAction="ifRoom" />

</menu>